import 'package:device_frame/device_frame.dart';
import 'package:flutter/cupertino.dart';

class DeviceModel{
  DeviceInfo device;
  IconData data;

  DeviceModel({required this.data,required this.device});
}